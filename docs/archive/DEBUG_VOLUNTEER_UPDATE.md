# 志愿者更新调试指南

## 🐛 问题
保存志愿者信息时报错：`Expected union value`

## 🔍 调试步骤

### 1. 查看浏览器控制台
打开浏览器开发者工具（F12），查看 Console 标签页，应该会看到：
```
🔍 更新志愿者数据: {
  "name": "...",
  "gender": "...",
  ...
}
```

### 2. 检查数据中的枚举字段
以下字段必须是指定的值之一，否则会报错：

#### gender (性别)
- ✅ 允许: `"male"`, `"female"`, `"other"`
- ❌ 不允许: 其他任何值、空字符串、null

#### education (学历)
- ✅ 允许: `"none"`, `"elementary"`, `"middle_school"`, `"high_school"`, `"bachelor"`, `"master"`, `"phd"`, `"other"`, `null`
- ❌ 不允许: 其他任何值、空字符串

#### refugeStatus (皈依状态)
- ✅ 允许: `"none"`, `"took_refuge"`, `"five_precepts"`, `"bodhisattva"`, `null`
- ❌ 不允许: 其他任何值、空字符串

#### healthConditions (健康状况)
- ✅ 允许: `"healthy"`, `"has_chronic_disease"`, `"has_disability"`, `"has_allergies"`, `"recovering_from_illness"`, `"other_conditions"`, `null`
- ❌ 不允许: 其他任何值、空字符串

#### religiousBackground (宗教背景)
- ✅ 允许: `"upasaka"`, `"upasika"`, `"sramanera"`, `"sramanerika"`, `"bhikkhu"`, `"bhikkhuni"`, `"anagarika"`, `"siladhara"`, `"novice_monk"`, `"buddhist_visitor"`, `"none"`, `null`
- ❌ 不允许: 其他任何值、空字符串

#### familyConsent (家属同意)
- ✅ 允许: `"approved"`, `"partial"`, `"rejected"`, `"self_decided"`, `null`
- ❌ 不允许: 其他任何值、空字符串

#### severPosition (服务岗位)
- ✅ 允许: `"kitchen"`, `"chanting"`, `"cleaning"`, `"reception"`, `"security"`, `"office"`, `"other"`, `null`
- ❌ 不允许: 其他任何值、空字符串

#### volunteerStatus (义工状态)
- ✅ 允许: `"applicant"`, `"trainee"`, `"registered"`, `"inactive"`, `"suspended"`, `null`
- ❌ 不允许: 其他任何值、空字符串

## 🔧 常见问题

### 问题 1: 空字符串
```json
{
  "education": ""  // ❌ 错误
}
```

**解决方案**: 改为 `null` 或不发送该字段
```json
{
  "education": null  // ✅ 正确
}
```

### 问题 2: 未定义的值
```json
{
  "gender": "unknown"  // ❌ 错误，不在允许的值中
}
```

**解决方案**: 使用允许的值
```json
{
  "gender": "other"  // ✅ 正确
}
```

### 问题 3: 布尔值字段
```json
{
  "hasBuddhismFaith": "true"  // ❌ 错误，应该是布尔值
}
```

**解决方案**: 使用布尔值
```json
{
  "hasBuddhismFaith": true  // ✅ 正确
}
```

## 📋 检查清单

请检查控制台打印的数据中，以下字段的值是否正确：

- [ ] `gender` - 必须是 "male", "female", "other" 之一
- [ ] `education` - 必须是允许的值或 null
- [ ] `refugeStatus` - 必须是允许的值或 null
- [ ] `healthConditions` - 必须是允许的值或 null
- [ ] `religiousBackground` - 必须是允许的值或 null
- [ ] `familyConsent` - 必须是允许的值或 null
- [ ] `severPosition` - 必须是允许的值或 null
- [ ] `volunteerStatus` - 必须是允许的值或 null
- [ ] `hasBuddhismFaith` - 必须是 true, false, 或 null

## 🛠️ 临时解决方案

如果找不到具体是哪个字段的问题，可以尝试：

1. **只更新基本信息**
   - 只修改姓名、电话等基本字段
   - 不修改枚举类型字段

2. **逐个字段测试**
   - 先保存基本信息
   - 然后逐个添加其他字段
   - 找出导致错误的字段

3. **查看后端日志**
   - 后端日志可能会显示具体是哪个字段的问题
   - 运行 `npm run dev` 查看实时日志

## 📞 需要帮助？

请提供以下信息：
1. 浏览器控制台打印的完整数据
2. 后端日志中的错误信息
3. 你修改了哪些字段

---

**提示**: 已在 `apps/web/src/services/volunteer.ts` 中添加了调试日志，保存时会在控制台打印发送的数据。
