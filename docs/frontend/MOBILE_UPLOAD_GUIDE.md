# 📱 手机扫码上传功能使用指南

## 功能说明

这是一个方便的手机-电脑照片传输功能，让管理员可以在电脑端操作时，使用手机拍照或选择照片上传。

## 使用场景

- 在电脑端编辑义工信息时，需要上传义工照片
- 手机上有现成的照片，不想传到电脑再上传
- 需要现场拍照上传

## 使用步骤

### 1. 在电脑端点击"手机扫码上传"按钮

在义工表单的照片上传区域，点击"手机扫码上传"按钮。

### 2. 扫描二维码

- 使用手机微信扫一扫
- 或使用手机浏览器扫描
- 或复制链接发送到手机

### 3. 在手机上上传照片

手机打开页面后，可以选择：
- **拍照上传** - 直接调用相机拍照
- **从相册选择** - 从手机相册选择已有照片

### 4. 自动同步到电脑

上传完成后，电脑端会自动接收照片，无需手动刷新。

## 技术实现

### 前端

1. **ImageUpload 组件** - 添加了"手机扫码上传"按钮
2. **MobileUploadDialog 组件** - 显示二维码和上传链接
3. **mobile-upload 路由** - 手机端的上传页面

### 后端 API

1. `POST /api/upload/token` - 生成上传令牌（10分钟有效）
2. `GET /api/upload/status/:token` - 检查上传状态（轮询）
3. `POST /api/upload/mobile` - 手机端上传接口（无需登录）

### 工作流程

```
电脑端                          后端                          手机端
  |                              |                              |
  |--1. 请求令牌---------------->|                              |
  |<--2. 返回令牌----------------|                              |
  |                              |                              |
  |--3. 生成二维码               |                              |
  |                              |                              |
  |                              |<--4. 扫码访问----------------|
  |                              |                              |
  |                              |<--5. 上传照片----------------|
  |                              |--6. 保存并关联令牌---------->|
  |                              |                              |
  |--7. 轮询检查状态------------>|                              |
  |<--8. 返回照片URL-------------|                              |
  |                              |                              |
  |--9. 显示照片                 |                              |
```

## 安全性

- 上传令牌10分钟后自动过期
- 每个令牌只能使用一次
- 手机端上传不需要登录，但需要有效令牌
- 定期清理过期令牌

## 文件限制

- 支持格式：JPG、PNG、WEBP
- 最大文件大小：5MB
- 自动生成唯一文件名

## 注意事项

1. 确保手机和电脑在同一网络环境下（或电脑端可被手机访问）
2. 二维码链接10分钟内有效
3. 上传完成后，电脑端会自动接收（每3秒检查一次）
4. 如果长时间未接收，可以关闭对话框重新扫码

## 依赖包

- `qrcode.react` - 生成二维码

## 未来优化

- [ ] 支持 WebSocket 实时推送（替代轮询）
- [ ] 支持批量上传多张照片
- [ ] 添加照片压缩功能
- [ ] 支持照片裁剪和旋转
- [ ] 添加上传进度显示
