/**
 * 照片压缩配置 - 简化版
 * 
 * 📌 简单规则：
 * - 小于 800KB → 不压缩
 * - 大于 800KB → 压缩至约 500KB
 * 
 * 💡 如何调整：
 * - 如果经常失败 → 提高 quality（95）或 targetSize（700）
 * - 如果网络较慢 → 降低 quality（85）或 targetSize（300）
 */

export const COMPRESSION_CONFIG = {
  /**
   * 压缩阈值（KB）
   * 超过此大小才压缩
   * 
   * 推荐值：800-1200
   */
  threshold: 800 * 1024,

  /**
   * 压缩质量（1-100）
   * 数值越高，质量越好，文件越大
   * 
   * 推荐值：
   * - 高质量（不容易失败）：95
   * - 平衡（推荐）：90
   * - 小文件（网络慢）：85
   */
  quality: 90,

  /**
   * 最大宽度（像素）
   * 照片宽度超过此值会缩小
   * 
   * 推荐值：
   * - 高质量：2560
   * - 平衡（推荐）：1920
   * - 小文件：1280
   */
  maxWidth: 1920,

  /**
   * 目标大小（KB）- 仅供参考
   * 实际大小取决于 quality 和 maxWidth
   */
  targetSize: 500 * 1024,

  // 兼容旧代码的策略配置（已废弃，不再使用）
  strategies: [],
  default: {
    quality: 90,
    maxWidth: 1920,
  },
}


